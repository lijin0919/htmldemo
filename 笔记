一、如何获取多个标签中特定的一个？
多个标签的对象[0];里面的数字代表你要的那一个标签的位置，从0开始
二、如何获取标签中的文字信息?
标签对象.innerHTML
三、如何获取input中输入的内容
标签.value
四、所有的通过name和标签名取得的是个标签大口袋

五、如何改变标签中的属性
标签.setAttribute("属性名称","属性值")


1、setAttribute（"属性名称","属性值"）
用来设置元素（标签）属性

2、createElement（"标签名称"）

3、appendChild（标签对象）

4、insertBefore（"新标签对象"，"谁的前面"）方法可在已有的子节点前插入一个新的子节点。

5、cloneNode（true/fa） 拷贝所有属性和值。